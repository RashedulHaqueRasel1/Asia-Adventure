
const MyListCard = ({u}) => {

    // console.log(info)
    const {name} = u;
    return (
        <div>

            <h1>card {name}</h1>
            
        </div>
    );
};

export default MyListCard;